{{- if .Values.serviceMesh.enabled }}
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: {{ include "edge-evidence.namespace" . }}
spec:
  mtls: {mode: STRICT}
{{- end }}
